<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="market">
	
	<!-- 검색조건 -->
	<sql id="search-list">
		<choose>
			<when test="condition == 'all'">
				( INSTR(productName, #{keyword}) &gt; 0
					OR INSTR(productInfo, #{keyword}) &gt; 0 )
			</when>
			<otherwise>
				INSTR(${condition}, #{keyword}) &gt; 0
			</otherwise>
		</choose>
	</sql>
	
	<!-- 데이터갯수 -->
	<select id="dataCount" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*), 0)
		FROM CorpProduct p
		JOIN member m ON m.memberIdx = p.memberIdx
		<where>
			<if test="keyword != null and keyword != '' ">
				<include refid="search-list"/>
			</if>
		</where>
	</select>
	
	<!-- 판매상품 리스트 -->
	<select id="listSales" parameterType="map" resultType="com.sp.app.market.Market">
		SELECT productName, imageFileName, salesPrice, registrationDate, statement
		FROM CorpProduct p
		JOIN member m ON m.memberIdx = p.memberIdx
		<where>
			<if test="keyword != null and keyword != ''">
				<include refid="search-list"/>
			</if>
		</where>
		ORDER BY registrationDate DESC
		OFFSET #{offset} ROWS FETCH FIRST #{rows} ROWS ONLY
	</select>
</mapper>